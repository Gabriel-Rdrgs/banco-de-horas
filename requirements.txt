# === Banco de Horas OCR — Provida Centro Médico ===
# Python 3.11 ou mais recente
# Instalação: pip install -r requirements.txt

# --- Interface web ---
streamlit>=1.30.0

# --- PDF e imagem ---
PyMuPDF>=1.23.0               # renderiza PDF sem depender do Poppler
opencv-python>=4.8.0          # remoção de grade + pré-processamento
Pillow>=10.0.0

# --- OCR em nuvem (Azure — engine ativo) ---
azure-ai-vision-imageanalysis>=1.0.0

# --- Excel ---
openpyxl>=3.1.0

# --- Dados ---
pandas>=2.0.0
python-dateutil>=2.8.0

# --- Configuração ---
python-dotenv>=1.0.0          # carrega credenciais do arquivo .env

# --- Engines alternativos (opcional) ---
# anthropic>=0.20.0           # Claude Vision
# google-cloud-vision>=3.0.0  # Google Vision
